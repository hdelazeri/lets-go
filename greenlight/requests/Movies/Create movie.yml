info:
  name: Create movie
  type: http
  seq: 1

http:
  method: POST
  url: "{{BASE_URL}}/v1/movies"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
