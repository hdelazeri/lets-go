info:
  name: Update movie
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{BASE_URL}}/v1/movies/:id"
  params:
    - name: id
      value: "4"
      type: path
  body:
    type: json
    data: |-
      {
          "year": 1985
        }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
