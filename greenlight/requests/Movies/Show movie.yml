info:
  name: Show movie
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL}}/v1/movies/:id"
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
