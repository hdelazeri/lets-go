info:
  name: Delete movie
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{BASE_URL}}/v1/movies/:id"
  params:
    - name: id
      value: "3"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
